{% for handler in handlers %} {% for method in handler.methods %}
def {{ method.name }}_{{ handler.name }}(Model):
    response = requests.{{ method.name }}({{ handler.url}})

    {% for pp in method.path_parameters %} {{ pp.name }} = {{ pp.real_type }}
    {% endfor %}
    {% for qp in method.query_parameters %} {{ qp.name }}  = {{ qp.real_type }}
    {% endfor %}
    {% for bp in method.body_parameters_defined %} body  = {{ bp }}()
    {% endfor %}
{% endfor %}
{% endfor %}
