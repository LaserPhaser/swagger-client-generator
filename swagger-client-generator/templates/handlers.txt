
{% for handler in handlers %}
    {% for method in handler.methods %}

def {{ method.name }}_{{ handler.name }}(Model):
    response = requests.{{ method.name }}({{ handler.url}})
    {% for p in method.path_parameters %}
    {{ p.name }} = {{p.real_type }} {% endfor %}


    {% for pp in method.query_parameters %}
    {{ pp.name }}  = {{ pp.real_type }} {% endfor %}

{% endfor %}
{% endfor %}
